% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/planetary.R
\name{planetary_boundaries}
\alias{planetary_boundaries}
\title{Planetary-boundary status as an ecological ceiling}
\usage{
planetary_boundaries(
  data,
  dimension = "boundary",
  value = "value",
  boundary = NULL,
  at = 1,
  relative = TRUE
)
}
\arguments{
\item{data}{a data.frame of boundary status, one row per boundary.}

\item{dimension}{name of the boundary-label column.}

\item{value}{name of the status column: a normalised control variable when
\code{boundary} is \code{NULL}, otherwise a raw control variable.}

\item{boundary}{optional. \code{NULL} for normalised input; otherwise a column name,
a single number, or one number per row giving each boundary's threshold.}

\item{at}{in normalised input, the value that sits on the boundary (default 1).}

\item{relative}{in raw input, whether overshoot is measured as a ratio to the
threshold (\code{TRUE}, default) or as a raw difference (\code{FALSE}).}
}
\value{
a data.frame with columns \code{dimension} and \code{overshoot}, ready to pass to
\code{\link[=doughnut]{doughnut()}} as its \code{ecological} argument.
}
\description{
A thin adapter that turns a table of planetary-boundary status into the
\code{ecological} data frame \code{\link[=doughnut]{doughnut()}} expects, so the Doughnut's ecological
ceiling can be driven from real boundary data rather than hand-entered
overshoots. It reads either normalised control variables (the Stockholm
Resilience Centre / Richardson et al. convention) or raw value-and-threshold
pairs, and returns one row per boundary with a \code{dimension} label and an
\code{overshoot} (0 within the boundary, larger beyond it).
}
\details{
Two input forms:
\itemize{
\item \strong{Normalised} (default, \code{boundary = NULL}). The \code{value} column holds the
control variable normalised so that the Holocene baseline sits at 0 and the
planetary boundary sits at \code{at} (1 by convention). This is the scale used in
the published planetary-boundaries figures and produced by the Potsdam
Institute \code{boundaries} package when control variables are normalised so the
boundary has the same radius for every process. Overshoot is
\code{pmax(0, value - at)}.
\item \strong{Raw}. Give a \code{boundary} column name (or a single number, or one number
per row) alongside a raw \code{value}. Overshoot is \code{value / boundary - 1} when
\code{relative = TRUE} (the sensible default, since control variables carry
different units) or \code{value - boundary} when \code{relative = FALSE}, clamped at 0.
}

\code{NA} values pass through as \code{NA}, so a boundary that is not quantified stays
pale in the Doughnut rather than being drawn as if it were within the safe
space.

This does not reproduce the Stockholm wedge diagram, and deliberately so: that
single-instant status wheel is already served by the Potsdam Institute
\code{boundaries} package (\code{plot_status()}), and drawing it a second time here would
add a static snapshot to a package whose point is that the boundary moves. Use
this to feed the ceiling; use \code{\link[=geom_boundary]{geom_boundary()}} to put the same boundaries back
on a time axis.
}
\examples{
# Normalised control variables (0 = Holocene baseline, 1 = at the boundary).
# Illustrative values in the spirit of the 2023 nine-boundary assessment.
pb <- data.frame(
  boundary = c("climate change", "biosphere integrity", "land-system change",
               "freshwater change", "nitrogen & phosphorus", "novel entities",
               "ocean acidification", "aerosol loading", "ozone depletion"),
  value    = c(1.4, 3.0, 1.2, 1.3, 2.4, 2.0, 0.9, 0.7, 0.3))
eco <- planetary_boundaries(pb)
eco

# Raw value against a threshold: atmospheric CO2 (ppm) vs a 350 ppm boundary.
raw <- data.frame(boundary = "climate change", co2 = 421, limit = 350)
planetary_boundaries(raw, value = "co2", boundary = "limit")
}
\seealso{
\code{\link[=doughnut]{doughnut()}} for the snapshot, \code{\link[=geom_boundary]{geom_boundary()}} for the same
boundaries over time.
}
